import{_ as i,o as e,c as a,ag as n}from"./chunks/framework.CtWPcAmt.js";const k=JSON.parse('{"title":"预加载脚本","description":"","frontmatter":{},"headers":[],"relativePath":"zhHans/electron-how-to/preload-script.md","filePath":"zhHans/electron-how-to/preload-script.md","lastUpdated":1788757155000}'),t={name:"zhHans/electron-how-to/preload-script.md"};function o(l,s,d,p,c,h){return e(),a("div",null,[...s[0]||(s[0]=[n(`<h1 id="预加载脚本" tabindex="-1">预加载脚本 <a class="header-anchor" href="#预加载脚本" aria-label="Permalink to &quot;预加载脚本&quot;">​</a></h1><p>Electron.js中的预加载脚本是一个安全区域，用于主进程和渲染器进程之间的通信。它通常用于 <strong><a href="https://www.electronjs.org/docs/latest/tutorial/ipc" target="_blank" rel="noreferrer">IPC通信</a></strong>。</p><p>更多信息，请参阅以下文章: <a href="https://www.electronjs.org/docs/latest/tutorial/tutorial-preload" target="_blank" rel="noreferrer">https://www.electronjs.org/docs/latest/tutorial/tutorial-preload</a></p><p>为了与最新版本的Electron兼容并确保安全，我们不建议使用旧的<code>electron/remote</code>模块。如果您想使用系统事件或Node脚本，建议在主进程中使用，而不是在渲染器中。</p><p>Vutron的预加载脚本位于<code>src/preload</code>文件夹中。要创建新的IPC通信通道，请将通道名称添加到以下变量中，将其列入通信白名单。</p><ul><li><code>mainSendChannels</code>: 从渲染进程向主进程发送事件，不等待回复。在主进程中使用 <code>ipcMain.on</code> 处理。 (<code>window.mainApi.send(&#39;channelName&#39;)</code>)</li><li><code>mainInvokeChannels</code>: 从渲染进程向主进程发送事件并等待结果。在主进程中使用 <code>ipcMain.handle</code> 处理。 (<code>window.mainApi.invoke(&#39;channelName&#39;)</code>)</li><li><code>rendererAvailChannels</code>: 从主进程向渲染进程发送事件。 (<code>mainWindow.webContents.send(&#39;channelName&#39;)</code>)</li></ul><p><code>send</code> 和 <code>invoke</code> 特意使用了各自独立的白名单，因此每个通道只能按照其处理程序的编写方式使用。</p><p>添加通道时，请同时在 <code>src/common/ipc.ts</code> 的 <code>MainSendPayloads</code>、<code>MainInvokeContracts</code> 或 <code>RendererEventPayloads</code> 中写明它传递的内容。只添加名称会导致构建失败。在主进程中，请使用 <code>src/main/utils/ipc.ts</code> 的 <code>handleInvoke</code>、<code>handleSend</code> 或 <code>sendToWindow</code> 注册，而不要直接调用 <code>ipcMain</code>。同一份契约会同时约束渲染器的调用处和主进程的处理函数。</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// src/common/ipc.ts</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> MainInvokeContracts</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  [</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">MAIN_INVOKE_CHANNELS</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.openFile]</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    args</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">filter</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">    result</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> OpenFileResult</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>当从渲染器向主程序发送事件时，应访问<code>window.mainApi</code>对象，而不是<code>ipcRenderer.send</code>。<code>mainApi</code>是您在自己的Vutron模板中设置的名称，可以更改。</p><p>以下是mainApi支持的功能:</p><ul><li><code>send</code>: 将活动发送至主页面。</li><li><code>on</code>: 一个接收主发送事件的听众。</li><li><code>once</code>: 接听主叫方发送的事件。（仅处理一个呼叫）</li><li><code>off</code>: 移除事件监听器</li><li><code>invoke</code>: 可异步发送事件和接收数据的功能。</li></ul><p>传给 <code>on</code> 或 <code>once</code> 的监听器只会收到调用 <code>webContents.send</code> 时传入的值。Electron 的 <code>IpcRendererEvent</code> 会留在预加载脚本中：它的 <code>sender</code> 是整个 <code>ipcRenderer</code>，把它交给渲染器就等于绕过上面的白名单，让渲染器可以使用任意通道。</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> unsubscribe</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> window.mainApi.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">on</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;msgWindowsUpdated&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">windowIds</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  console.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">log</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(windowIds)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">})</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 不再需要该监听器时调用</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">unsubscribe</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span></span></code></pre></div><p><code>on</code> 和 <code>once</code> 会返回一个用于移除监听器的函数。如果监听器是在组件中注册的，请在 <code>onUnmounted</code> 中调用它：监听器位于主进程，不会随着界面消失而被清理。把同一个监听器传给 <code>off</code> 也可以。</p><p>要更改和修改此设置，您需要修改 <code>src/preload/index.ts</code> 中的 <code>exposeInMainWorld</code>。</p>`,16)])])}const E=i(t,[["render",o]]);export{k as __pageData,E as default};
