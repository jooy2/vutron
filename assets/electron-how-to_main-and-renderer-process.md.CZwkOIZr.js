import{_ as s,o as t,c as n,ag as a}from"./chunks/framework.CtWPcAmt.js";const g=JSON.parse('{"title":"Main vs Renderer Process","description":"","frontmatter":{},"headers":[],"relativePath":"electron-how-to/main-and-renderer-process.md","filePath":"en/electron-how-to/main-and-renderer-process.md","lastUpdated":1786168557000}'),o={name:"electron-how-to/main-and-renderer-process.md"};function r(i,e,d,c,p,h){return t(),n("div",null,[...e[0]||(e[0]=[a(`<h1 id="main-vs-renderer-process" tabindex="-1">Main vs Renderer Process <a class="header-anchor" href="#main-vs-renderer-process" aria-label="Permalink to &quot;Main vs Renderer Process&quot;">​</a></h1><p>A <strong>Vutron</strong> application is divided into code into a Main process and a Renderer process.</p><p><strong>&quot;Main&quot;</strong> is the code of <code>src/main</code> and is mainly the process code handled by Electron. <strong>&quot;Renderer&quot;</strong> is the code of <code>src/renderer</code>, mainly for front-end rendering process like Vue.</p><p>In general, <strong>Node.js</strong> scripts cannot be run in the renderer process. Examples include modules that contain APIs used by Node.js, or native modules of <strong>Node.js</strong> such as <code>path</code> or <code>net</code>, <code>os</code> or <code>crypto</code>.</p><p>Preload scripts are run before the renderer is loaded. It creates a bridge to the main process to keep the execution of Node.js scripts in the renderer area separate and isolated for security reasons.</p><p>For secure script execution, it is recommended that the main process executes the Node scripts, and the renderer receives the execution results via messaging. This can be implemented via <strong>IPC communication</strong>.</p><p>For more information on this, see the following articles: <a href="https://www.electronjs.org/docs/latest/tutorial/ipc" target="_blank" rel="noreferrer">https://www.electronjs.org/docs/latest/tutorial/ipc</a></p><h2 id="sharing-code-between-the-processes" tabindex="-1">Sharing code between the processes <a class="header-anchor" href="#sharing-code-between-the-processes" aria-label="Permalink to &quot;Sharing code between the processes&quot;">​</a></h2><p>Some code belongs to neither side: an IPC channel name, a payload type, a validation rule, a pure helper. Anything of that kind goes in <code>src/common</code>, which the main, the preload and the renderer build all import through the <code>@</code> alias.</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// src/common/ipc.ts</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> MAIN_INVOKE_CHANNELS</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  requestGetVersion: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;msgRequestGetVersion&#39;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">} </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">as</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> const</span></span></code></pre></div><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Same import in src/main, src/preload and src/renderer</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { MAIN_INVOKE_CHANNELS } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@/common/ipc&#39;</span></span></code></pre></div><p>The template ships two of them: <code>common/ipc.ts</code> holds the channel names the three processes agree on, along with the types of what travels over them, and <code>common/locales.ts</code> holds the language list and the tag matching that goes with it.</p><p>Two things to keep in mind when adding your own:</p><ul><li><strong>Only what runs everywhere.</strong> The renderer has no Node.js and no Electron, the main process has no DOM and no Vue. So <code>src/common</code> may not import a Node.js builtin, <code>electron</code> or a renderer framework, and may not touch <code>window</code>, <code>navigator</code> or <code>process</code>. Type-only imports (<code>import type { OpenDialogReturnValue } from &#39;electron&#39;</code>) are erased at build time and are fine. ESLint fails the build on the rest, so a mistake shows up while you write it rather than at runtime.</li><li><strong>Each process gets its own copy.</strong> The three bundles are built separately, so a variable exported from <code>src/common</code> is not one shared value: changing it in the renderer leaves the main process copy untouched. Keep <code>src/common</code> to constants, types and pure functions, and pass state over IPC.</li></ul><h3 id="how-to-run-node-js-on-a-renderer" tabindex="-1">How to run Node.js on a renderer? <a class="header-anchor" href="#how-to-run-node-js-on-a-renderer" aria-label="Permalink to &quot;How to run Node.js on a renderer?&quot;">​</a></h3><p>If you want to skip the security issues and use Node.js scripts in your renderer, you need to set <code>nodeIntegration</code> to <code>true</code> in your <code>vite.config.ts</code> file.</p><div class="language-javascript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">javascript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">rendererPlugin</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  nodeIntegration: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">})</span></span></code></pre></div><p>For more information on this, see the following articles: <a href="https://github.com/electron-vite/vite-plugin-electron-renderer" target="_blank" rel="noreferrer">https://github.com/electron-vite/vite-plugin-electron-renderer</a></p>`,18)])])}const u=s(o,[["render",r]]);export{g as __pageData,u as default};
